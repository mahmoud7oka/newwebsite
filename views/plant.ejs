<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title><%= plant["اسم"] %> - الأمراض</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
  <style>
    * {
      font-family: 'Cairo', sans-serif;
    }
    body {
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      color: white;
      direction: rtl;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: rgba(0,0,0,0.6);
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .visitor-count {
      font-size: 16px;
      color: #ccc;
    }
    .container {
      max-width: 800px;
      margin: 40px auto;
      background: rgba(0, 0, 0, 0.5);
      padding: 30px;
      border-radius: 16px;
    }
    h1 {
      font-size: 28px;
      text-align: center;
      margin-bottom: 10px;
    }
    .info {
      font-size: 18px;
      text-align: center;
      margin-bottom: 20px;
    }
    ul.diseases {
      list-style: none;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    ul.diseases li a {
      background-color: rgba(255,255,255,0.1);
      color: white;
      padding: 12px 20px;
      display: block;
      border-radius: 10px;
      text-align: center;
      text-decoration: none;
      transition: 0.3s;
      font-size: 18px;
    }
    ul.diseases li a:hover {
      background-color: rgba(255,255,255,0.3);
    }
    .nav-buttons {
      text-align: center;
      margin-top: 30px;
    }
    .nav-buttons a {
      margin: 0 10px;
      background: rgba(255,255,255,0.1);
      padding: 10px 20px;
      border-radius: 10px;
      color: white;
      font-size: 16px;
      text-decoration: none;
    }
    .nav-buttons a:hover {
      background: rgba(255,255,255,0.3);
    }
  </style>
</head>
<body>
  <header>
    <h2>🌿 مساعد الزراعة</h2>
    <span class="visitor-count">👥 عدد الزوار: <%= visitors %></span>
  </header>

  <div class="container">
    <h1><%= plant["اسم"] %></h1>
    <div class="info">
      <p>📚 القسم: <%= plant["قسم"] %></p>
      <p>☀️ الفصل: <%= plant["الفصل"] %></p>
    </div>

    <ul class="diseases">
      <% diseases.forEach(disease => { %>
        <li>
          <a href="/plant/<%= encodeURIComponent(plant['اسم']) %>/disease/<%= encodeURIComponent(disease) %>">
            🦠 <%= disease %>
          </a>
        </li>
      <% }) %>
    </ul>

    <div class="nav-buttons">
      <a href="/">🏠 القائمة الرئيسية</a>
      <a href="javascript:history.back()">🔙 رجوع</a>
    </div>
  </div>
</body>
</html>
