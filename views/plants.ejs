<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>قائمة النباتات</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&family=Noto+Color+Emoji" rel="stylesheet">
  <style>
    body {
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      color: white;
      direction: rtl;
      font-family: 'Cairo', 'Noto Color Emoji', sans-serif;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .visitor-count {
      font-size: 16px;
      color: #e0e0e0;
    }
    .container {
      max-width: 1000px;
      margin: 50px auto;
      background: rgba(0, 0, 0, 0.4);
      padding: 30px;
      border-radius: 16px;
    }
    h1 {
      text-align: center;
      font-size: 30px;
      margin-bottom: 30px;
    }
    .plant-list {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
    }
    .plant-card {
      background: rgba(255, 255, 255, 0.1);
      padding: 20px;
      border-radius: 12px;
      text-align: center;
      transition: 0.3s;
    }
    .plant-card:hover {
      background: rgba(255, 255, 255, 0.25);
    }
    .plant-card a {
      text-decoration: none;
      color: #fff;
      font-size: 18px;
      font-weight: bold;
    }
    .plant-card .info {
      margin-top: 10px;
      font-size: 15px;
      color: #ccc;
    }
    .nav-buttons {
      text-align: center;
      margin-top: 40px;
    }
    .nav-buttons a {
      background: rgba(255,255,255,0.1);
      padding: 10px 20px;
      border-radius: 10px;
      color: white;
      font-size: 16px;
      font-weight: bold;
      transition: 0.2s;
      text-decoration: none;
      margin: 0 10px;
    }
    .nav-buttons a:hover {
      background: rgba(255,255,255,0.3);
    }
  </style>
</head>
<body>

  <header>
    <h2>🌿 مساعد الزراعة</h2>
    <span class="visitor-count">👥 عدد الزوار: <%= visitors %></span>
  </header>

  <div class="container">
    <h1>🪴 قائمة النباتات</h1>
    <div class="plant-list">
      <% data.forEach(plant => { %>
        <div class="plant-card">
          <a href="/plant/<%= encodeURIComponent(plant['اسم']) %>"><%= plant['اسم'] %></a>
          <div class="info">القسم: <%= plant['قسم'] %></div>
          <div class="info">الفصل: <%= plant['الفصل'] %></div>
        </div>
      <% }); %>
    </div>

    <div class="nav-buttons">
      <a href="/">🏠 القائمة الرئيسية</a>
      <a href="javascript:history.back()">🔙 رجوع</a>
    </div>
  </div>

</body>
</html>
